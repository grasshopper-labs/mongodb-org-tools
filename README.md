# Install MongoDB Tools Action

This GitHub Action installs MongoDB Database Tools (e.g., `mongodump`, `mongorestore`) on the runner.

## Usage

```yaml
- name: Install MongoDB Tools
  uses: <your-username>/install-mongo-tools-action@v1
